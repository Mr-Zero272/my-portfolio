import { GalleryImageListPage } from '@/features/gallery';

export const metaData = {
  title: 'Galleries',
  description: 'Manage gallery images',
};

export default function GalleriesPage() {
  return <GalleryImageListPage />;
}
