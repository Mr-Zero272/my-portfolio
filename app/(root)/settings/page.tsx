import UserSettings from '@/features/settings/pages/user-settings';

export const metadata = {
  title: 'Settings',
  description: 'Custom theme, effect and more',
};
const SettingsPage = () => {
  return <UserSettings />;
};

export default SettingsPage;
